worker_processes auto;

events {
    worker_connections 1024;
}

http {
    # Include MIME types
    include       mime.types;
    default_type  application/octet-stream;
    sendfile      on;
    keepalive_timeout 65;

    server {
      listen 80;
      server_name localhost;

      root /usr/share/nginx/html;
      index index.html;

      location / {
          try_files $uri $uri/ /index.html;
      }

      error_page 500 502 503 504 /50x.html;
      location = /50x.html {
          root /usr/share/nginx/html;
      }
    }

    # ---------------------------
    # FRONTEND HTTP -> HTTPS redirect
    # ---------------------------
    # server {
    #     listen 80;
    #     listen [::]:80;
    #     server_name frontend.listit.avinashjha.space www.frontend.listit.avinashjha.space;

    #     return 301 https://$host$request_uri;
    # }

    # # ---------------------------
    # # BACKEND HTTP -> HTTPS redirect
    # # ---------------------------
    # server {
    #     listen 80;
    #     listen [::]:80;
    #     server_name api.backend.listit.avinashjha.space;

    #     return 301 https://$host$request_uri;
    # }

    # ---------------------------
    # FRONTEND HTTPS
    # ---------------------------
    # server {
    #     listen 443 ssl;
    #     listen [::]:443 ssl;
    #     server_name frontend.listit.avinashjha.space www.frontend.listit.avinashjha.space;

    #     ssl_certificate /etc/ssl/certs/cert.pem;
    #     ssl_certificate_key /etc/ssl/certs/key.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;

    #     location / {
    #         proxy_pass http://remix-frontend:3000;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }

    # ---------------------------
    # BACKEND HTTPS
    # ---------------------------
    # server {
    #     listen 443 ssl;
    #     listen [::]:443 ssl;
    #     server_name api.backend.listit.avinashjha.space;

    #     ssl_certificate /etc/ssl/certs/cert.pem;
    #     ssl_certificate_key /etc/ssl/certs/key.pem;
    #     ssl_protocols TLSv1.2 TLSv1.3;
    #     ssl_ciphers HIGH:!aNULL:!MD5;

    #     location / {
    #         proxy_pass http://backend:5000;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #     }
    # }

    # # ---------------------------
    # # FALLBACK / DEFAULT SERVER
    # # ---------------------------
    # server {
    #     listen 443 ssl default_server;
    #     listen [::]:443 ssl default_server;
    #     server_name _;

    #     ssl_certificate /etc/ssl/certs/cert.pem;
    #     ssl_certificate_key /etc/ssl/certs/key.pem;

    #     return 444; # Drop unknown connections
    # }
}
