1. encrypt user storage credentials in database:  - done
  - encrypted credentails in encryptedCredentials field in db
  - created encryption service to handle encryption and decryption
  - updated proxy also to decrypt credentials while fetching from db
  - updated onboarding service to store empty credentials object in plain credentials field while storing encrypted credentials in encryptedCredentials field

2. valdiation of storage credentials after onboarding:  - done
  - after user completes onboarding, validate the provided storage credentials
  - if valid, mark isValidated true
  - if invalid, store error message in validationError field and return error with message, details and suggestions to user

2. theme update
3. fix cors error in generate api keys
4. fix developer console ui design
5. add images document video pages - done
6. show storage provider region code names in select labels - done